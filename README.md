# 湾区代表队行程联控

本项目协调粤港澳体育代表队在比赛、训练、校园交流、产业参访和主题市集之间的行程。名单、通行凭证、活动时段、车辆和场地容量各自维护版本，调度结果应尊重已经开始的行程。

`fixtures/sample.json` 描述两天全明星赛和四支参赛队伍的基础安排。现场通行记录只使用参与者别名与凭证标识，不保存证件图像。

可运行 `python3 service.py --check` 确认项目入口，`python3 -m unittest -v` 用于核对服务身份和赛程边界。启动后通过 `/health` 查看健康状态。
